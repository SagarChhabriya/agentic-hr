[project]
name = "agentic-hr-interview-agent"
version = "0.1.0"
description = "LiveKit voice agent for AI interviews (Groq LLM + Deepgram STT/TTS)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "livekit-agents[deepgram,groq,silero,turn-detector]~=1.4",
    "livekit-plugins-noise-cancellation~=0.2",
    "python-dotenv>=1.0.0",
]

[project.optional-dependencies]
dev = []

[tool.uv]
